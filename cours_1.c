/*Uriel */
// Uriel: Inclut la bibliothèque stdio pour les entrées/sorties
#include <stdio.h>
// Uriel: Inclut la bibliothèque stdlib pour les fonctions utilitaires
#include <stdlib.h>

// Uriel: Déclare la fonction principale qui ne retourne rien et ne prend aucun paramètre
int main(void)
{
    // Uriel: Déclare une variable entière a et l'initialise à 5
    int a = 5;
    // Uriel: Déclare une variable entière b et l'initialise à la valeur de a plus 1 (donc 6)
    int b=a+1;
    // Uriel: Déclare une variable entière c et l'initialise à la somme de a et b (donc 11)
    int c=a+b;
}